'use client';
import {FormEvent,useMemo,useState} from 'react'; import Link from 'next/link';
const slug=(s:string)=>s.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]/g,'');
export default function NovoTime(){const[name,setName]=useState('');const[responsible,setResponsible]=useState('');const[phone,setPhone]=useState('');const[campo,setCampo]=useState(false);const[futsal,setFutsal]=useState(false);const[msg,setMsg]=useState('');const login=useMemo(()=>slug(name),[name]);function submit(e:FormEvent){e.preventDefault();setMsg('Para salvar no banco, entre primeiro como Super ADM. A autenticação será ativada na próxima etapa.')}return <main className="formPage"><div className="formTop"><Link href="/admin">← Voltar</Link><div className="brand"><span className="shield">V10</span><strong>VARZ10</strong></div></div><section className="teamForm"><p className="eyebrow">SUPER ADM</p><h1>Cadastrar time</h1><p className="muted">Crie a conta e escolha quais modalidades o responsável poderá acessar.</p><form onSubmit={submit}><label>Nome do time<input required value={name} onChange={e=>setName(e.target.value)} placeholder="Ex: Aliados da Parma"/></label><label>Login do time<input value={login} readOnly placeholder="gerado automaticamente"/><small>Somente o Super ADM poderá alterar este login.</small></label><div className="twoCols"><label>Nome do responsável<input required value={responsible} onChange={e=>setResponsible(e.target.value)} placeholder="Nome completo"/></label><label>Celular / WhatsApp<input value={phone} onChange={e=>setPhone(e.target.value)} placeholder="(11) 99999-9999"/></label></div><fieldset><legend>Modalidades liberadas</legend><label className="check"><input type="checkbox" checked={campo} onChange={e=>setCampo(e.target.checked)}/> Futebol de Campo</label><label className="check"><input type="checkbox" checked={futsal} onChange={e=>setFutsal(e.target.checked)}/> Futsal / Quadra</label></fieldset><div className="firstAccess"><b>Primeiro acesso</b><p>O responsável receberá o login acima e criará a própria senha no primeiro acesso.</p></div>{msg&&<div className="firstAccess"><p>{msg}</p></div>}<button className="primary full" type="submit">Cadastrar time</button></form></section></main>}
